name: C/C++ CI

on: workflow_dispatch


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Install Dependencies
      run: apt install -y ${{ steps.package.outputs.content }}
    - name: Read package list
      uses: ./
      id: package
      with:
        path: packages.txt
    - name: Checkout
      uses: actions/checkout@v2
